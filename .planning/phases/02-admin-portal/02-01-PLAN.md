---
phase: 02-admin-portal
plan: 01
type: execute
wave: 1
depends_on: ["01-02"]
files_modified: [src/app/pages/StudentManagement.tsx, src/app/pages/AdminTeachers.tsx]
autonomous: true
requirements: [ADMIN-01, ADMIN-03]
must_haves:
  truths:
    - "Admin can view, create, edit, and delete Student records"
    - "Admin can view, create, edit, and delete Instructor records"
    - "UI matches the 02-UI-SPEC.md contract"
  artifacts:
    - path: "src/app/pages/StudentManagement.tsx"
      provides: "Student CRUD interface"
    - path: "src/app/pages/AdminTeachers.tsx"
      provides: "Instructor CRUD interface"
  key_links:
    - "StudentManagement UI calls Supabase student table"
    - "AdminTeachers UI calls Supabase instructor table"
---

<objective>
Refactor the Student and Instructor management pages to use standardized Shadcn UI components while maintaining their Supabase API connections.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/02-UI-SPEC.md
@src/app/pages/StudentManagement.tsx
@src/app/pages/AdminTeachers.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Install Shadcn Dependencies</name>
  <action>Run `npx shadcn@latest add table dialog sheet form input label button select` in the root directory. Ensure all components are added successfully to `src/components/ui/`.</action>
  <verify>ls src/components/ui/table.tsx</verify>
  <acceptance_criteria>
    - Required shadcn components are present in the project.
  </acceptance_criteria>
  <done>UI components initialized.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor StudentManagement.tsx</name>
  <files>src/app/pages/StudentManagement.tsx</files>
  <read_first>.planning/02-UI-SPEC.md, src/app/pages/StudentManagement.tsx</read_first>
  <action>Refactor the custom HTML table to use the Shadcn `Table` component. Replace the custom modal with Shadcn `Dialog` or `Sheet` for the Add/Edit forms. Verify the code still correctly calls `getStudents`, `createStudent`, `updateStudent`, `deleteStudent` from `src/lib/api.ts`.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - Student CRUD uses Shadcn Table and Dialog/Sheet.
    - Matches styling from `02-UI-SPEC.md`.
  </acceptance_criteria>
  <done>Student CRUD interface modernized.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor AdminTeachers.tsx</name>
  <files>src/app/pages/AdminTeachers.tsx</files>
  <read_first>.planning/02-UI-SPEC.md, src/app/pages/AdminTeachers.tsx</read_first>
  <action>Refactor the instructor CRUD interface similarly. It must use Shadcn `Table` for displaying records and `Dialog` or `Sheet` for adding/editing instructors. Connect to the instructor API endpoints.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - Instructor CRUD uses Shadcn Table and Dialog/Sheet.
    - Matches styling from `02-UI-SPEC.md`.
  </acceptance_criteria>
  <done>Instructor CRUD interface modernized.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles successfully
</verification>

<success_criteria>
- Both Student and Instructor pages feature Shadcn components for Data display and Modals.
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-portal/02-01-SUMMARY.md`
</output>
