---
phase: 02-admin-portal
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/app/pages/AdminCourses.tsx]
autonomous: true
requirements: [ADMIN-02, ADMIN-04]
must_haves:
  truths:
    - "Admin can view, create, edit, and delete Course records"
    - "Admin can enroll students into specific courses"
  artifacts:
    - path: "src/app/pages/AdminCourses.tsx"
      provides: "Course CRUD and Enrollment interface"
  key_links:
    - "AdminCourses UI calls Supabase courses table"
    - "Enrollment action updates students course field"
---

<objective>
Refactor the Course management page to use Shadcn components and implement the student enrollment workflow (updating the student's `course` field).
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/02-UI-SPEC.md
@src/app/pages/AdminCourses.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Refactor AdminCourses.tsx</name>
  <files>src/app/pages/AdminCourses.tsx</files>
  <read_first>.planning/02-UI-SPEC.md, src/app/pages/AdminCourses.tsx</read_first>
  <action>Refactor the course list to use a Shadcn `Table`. Implement Shadcn `Dialog` or `Sheet` for adding and editing courses. Connect to Supabase course API logic.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - Course CRUD uses Shadcn Table and Dialog/Sheet.
  </acceptance_criteria>
  <done>Course CRUD interface modernized.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Enrollment Workflow</name>
  <files>src/app/pages/AdminCourses.tsx</files>
  <read_first>src/app/pages/AdminCourses.tsx</read_first>
  <action>Add an "Enroll Students" action for each course. This should open a `Sheet` or `Dialog` that lists available students (fetched from the students API). The Admin should be able to select students and "Enroll" them, which triggers an update to the selected students' `course` field, assigning them to the current course name.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - Admin can enroll multiple students into a course.
    - Enrollment strictly updates the student `course` column.
  </acceptance_criteria>
  <done>Enrollment workflow implemented.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles successfully
</verification>

<success_criteria>
- Course management supports CRUD and student enrollment via Shadcn interfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-portal/02-02-SUMMARY.md`
</output>
