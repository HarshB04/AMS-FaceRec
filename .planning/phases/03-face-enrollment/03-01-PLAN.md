---
phase: 03-face-enrollment
plan: 01
type: execute
wave: 1
depends_on: ["02-02"]
files_modified: [src/app/pages/StudentManagement.tsx]
autonomous: true
requirements: [ADMIN-05]
must_haves:
  truths:
    - "Admin has a dedicated UI for launching face enrollment."
    - "Python engine captures and stores student faces securely."
  artifacts:
    - path: "src/app/pages/StudentManagement.tsx"
      provides: "Face enrollment error handling and UI"
  key_links:
    - "StudentManagement UI pings localhost:5000/enroll_feed"
---

<objective>
Formalize the Admin Face Enrollment UI that was integrated during Phase 2. Add proper error handling in the React frontend in case the local Python Flask server is not running.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/pages/StudentManagement.tsx
@face_engine/server.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Python Server Error Handling to StudentManagement</name>
  <files>src/app/pages/StudentManagement.tsx</files>
  <read_first>src/app/pages/StudentManagement.tsx</read_first>
  <action>Modify `handleFlaskEnroll` in `StudentManagement.tsx`. Before setting `setEnrolling(true)`, ping `http://localhost:5000/` to verify the Flask server is running. If the fetch fails, alert the user "Python Face Engine is not running. Please start the server on port 5000."</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - User gets a clear alert if they try to enroll a face but the python server is offline.
  </acceptance_criteria>
  <done>Frontend gracefully handles Python server unavailability.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles successfully
</verification>

<success_criteria>
- Face enrollment UI exists and gracefully handles server connection failures.
</success_criteria>

<output>
After completion, create `.planning/phases/03-face-enrollment/03-01-SUMMARY.md`
</output>
