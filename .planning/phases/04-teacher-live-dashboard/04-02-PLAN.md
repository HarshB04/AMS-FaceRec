---
phase: 04-teacher-live-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/app/lib/api.ts, src/app/pages/LiveCamera.tsx]
autonomous: true
requirements: [TCHR-02, TCHR-03]
must_haves:
  truths:
    - "LiveCamera dashboard correctly reflects total students."
    - "LiveCamera UI automatically updates Recognized list from Supabase attendance."
  artifacts:
    - path: "src/app/pages/LiveCamera.tsx"
      provides: "Realtime attendance view"
---

<objective>
Connect the LiveCamera component to both the Python Engine and Supabase Realtime to render an accurate, live view of a class session.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/app/lib/api.ts
@src/app/pages/LiveCamera.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add getTodayAttendanceForCourse</name>
  <files>src/app/lib/api.ts</files>
  <action>Add `getTodayAttendanceForCourse(courseCode: string)` to fetch attendance records logged today for that course.</action>
  <verify>tsc --noEmit passes</verify>
  <done>API exposes today's attendance.</done>
</task>

<task type="auto">
  <name>Task 2: Real Data in LiveCamera</name>
  <files>src/app/pages/LiveCamera.tsx</files>
  <action>Update `LiveCamera.tsx` to accept the course code from URL params (`?course=CS-301`). Fetch `totalStudents` using `getStudentsByCourse`. Instead of fully relying on `/recognized`, poll `getTodayAttendanceForCourse` to build the "Recognized" list accurately. Update metrics.</action>
  <verify>tsc --noEmit passes</verify>
  <done>LiveCamera shows actual real-time class data.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles successfully
</verification>

<success_criteria>
- The live dashboard accurately reflects total and present students for the selected course.
</success_criteria>

<output>
After completion, create `.planning/phases/04-teacher-live-dashboard/04-02-SUMMARY.md`
</output>
