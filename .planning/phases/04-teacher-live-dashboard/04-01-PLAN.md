---
phase: 04-teacher-live-dashboard
plan: 01
type: execute
wave: 1
depends_on: ["03-01"]
files_modified: [src/app/lib/api.ts, src/app/pages/TeacherClasses.tsx]
autonomous: true
requirements: [TCHR-01]
must_haves:
  truths:
    - "Teacher sees real active courses and enrolled students, not dummy data."
  artifacts:
    - path: "src/app/pages/TeacherClasses.tsx"
      provides: "Course and Roster rendering"
  key_links:
    - "TeacherClasses.tsx uses getStudentsByCourse"
---

<objective>
Replace the dummy static data in `TeacherClasses.tsx` with live data from Supabase.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/app/lib/api.ts
@src/app/pages/TeacherClasses.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add getStudentsByCourse to API</name>
  <files>src/app/lib/api.ts</files>
  <action>Add `getStudentsByCourse(courseCode: string)` to fetch students where `course` matches the given courseCode.</action>
  <verify>tsc --noEmit passes</verify>
  <done>API exposes real student rosters.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Real Roster in TeacherClasses</name>
  <files>src/app/pages/TeacherClasses.tsx</files>
  <action>Remove the `ROSTER` constant. Make `ClassCard` fetch real students using `getStudentsByCourse(course.code)` on mount. Calculate aggregate attendance correctly.</action>
  <verify>tsc --noEmit passes</verify>
  <done>TeacherClasses no longer relies on dummy data.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles successfully
</verification>

<success_criteria>
- The Teacher Classes page shows live students from the database.
</success_criteria>

<output>
After completion, create `.planning/phases/04-teacher-live-dashboard/04-01-SUMMARY.md`
</output>
