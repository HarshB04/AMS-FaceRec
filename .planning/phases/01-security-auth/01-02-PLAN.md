---
phase: 01-security-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [supabase/schema.sql]
autonomous: true
requirements: [AUTH-03, AUTH-04]
must_haves:
  truths:
    - "RLS prevents unauthorized data access"
    - "JWT role spoofing is impossible"
  artifacts:
    - path: "supabase/schema.sql"
      provides: "Row Level Security policies"
  key_links: []
---

<objective>
Implement strict RLS policies on all tables.

Purpose: Frontend UI hiding is not security. We must enforce database-level security using PostgreSQL Row Level Security (RLS) to ensure that even if the React app is bypassed, data is inaccessible.
Output: Hardened `schema.sql` file.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@supabase/schema.sql
</context>

<schema_push_requirement>
**[BLOCKING] Schema Push Required**

This phase modifies schema-relevant files (schema.sql). The planner MUST include
a `[BLOCKING]` task that runs the database schema push command AFTER all schema file
modifications are complete but BEFORE verification.

- ORM detected: Supabase
- Push command: supabase db push
- Non-TTY workaround: Set SUPABASE_ACCESS_TOKEN env var or run manually if needed.
</schema_push_requirement>

<tasks>

<task type="auto">
  <name>Task 1: Harden Row Level Security</name>
  <files>supabase/schema.sql</files>
  <read_first>supabase/schema.sql</read_first>
  <action>Review `supabase/schema.sql`. Ensure `ALTER TABLE [table] ENABLE ROW LEVEL SECURITY;` is applied to `students`, `courses`, `instructors`, and `attendance`. Create policies that verify roles using the JWT: `auth.jwt() -> 'user_metadata' ->> 'role'`. For example, `admin` can ALL, `teacher` can SELECT on courses/attendance, `student` can SELECT their own records.</action>
  <verify>grep "ENABLE ROW LEVEL SECURITY" supabase/schema.sql</verify>
  <acceptance_criteria>
    - `schema.sql` enables RLS for all data tables.
    - Policies explicitly check `auth.jwt() -> 'user_metadata' ->> 'role'`.
  </acceptance_criteria>
  <done>Database is secured against unauthorized access.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push Schema to Supabase</name>
  <files>supabase/schema.sql</files>
  <read_first>supabase/schema.sql</read_first>
  <action>Since Supabase requires a linked project to push via CLI, prompt the user to manually execute the `schema.sql` statements in their Supabase SQL Editor to apply the RLS updates to the live database.</action>
  <verify>User confirms execution.</verify>
  <acceptance_criteria>
    - User confirms the schema has been executed on the remote database.
  </acceptance_criteria>
  <done>Remote database RLS is active.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Schema applies without errors in Postgres
- [ ] All required tables have RLS enabled
</verification>

<success_criteria>
- All tasks completed
- Database schema is hardened and deployed
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-auth/01-02-SUMMARY.md`
</output>
