---
phase: 01-security-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/pages/LoginPage.tsx, src/app/components/layout/AuthGuard.tsx]
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "User can log in with email and password"
    - "Unauthorized users are redirected away from protected routes"
    - "UI matches the 01-UI-SPEC.md contract"
  artifacts:
    - path: "src/app/pages/LoginPage.tsx"
      provides: "Login UI and Supabase Auth integration"
    - path: "src/app/components/layout/AuthGuard.tsx"
      provides: "Role-based route wrapping"
  key_links: []
---

<objective>
Configure Supabase Auth UI and Role-Based Routing.

Purpose: To secure the application frontend, ensuring only authenticated users can access the dashboard, and they are strictly routed based on their role (`admin`, `teacher`, `student`).
Output: Fully functioning Login page and AuthGuard wrapper.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/01-UI-SPEC.md

@src/app/pages/LoginPage.tsx
@src/app/components/layout/AuthGuard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Solidify LoginPage.tsx</name>
  <files>src/app/pages/LoginPage.tsx</files>
  <read_first>src/app/pages/LoginPage.tsx, .planning/01-UI-SPEC.md</read_first>
  <action>Review the existing `LoginPage.tsx` implementation. Ensure it uses `supabase.auth.signInWithPassword`. Verify that on successful login, the user's `role` is extracted from `user_metadata` (defaulting to 'student' if missing) and the user is redirected to the dashboard. Ensure the UI styling matches the constraints in `01-UI-SPEC.md` (Shadcn components, Slate/Blue accents).</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - `src/app/pages/LoginPage.tsx` contains `supabase.auth.signInWithPassword`
    - Form handles loading and error states properly
  </acceptance_criteria>
  <done>Login page securely authenticates against Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Solidify AuthGuard.tsx</name>
  <files>src/app/components/layout/AuthGuard.tsx</files>
  <read_first>src/app/components/layout/AuthGuard.tsx, .planning/01-UI-SPEC.md</read_first>
  <action>Review `AuthGuard.tsx`. Ensure it correctly queries `supabase.auth.getSession()` on mount. It must verify the session exists AND that `session.user.user_metadata.role` matches the expected `role` prop passed to the component. If unauthorized, redirect to `/login` or show the Unauthorized text from `01-UI-SPEC.md` ("You are not authorized to view this page. Redirecting...").</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - `AuthGuard.tsx` checks `user_metadata.role`
    - Redirects to login on failure
  </acceptance_criteria>
  <done>Routes are protected by strict role checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles successfully
- [ ] Visual verification of login page against UI-SPEC
</verification>

<success_criteria>
- All tasks completed
- Authentication flow is secure and complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-auth/01-01-SUMMARY.md`
</output>
